!***> Setup

| '''Setup''' | !-NewVersionNotifierNoChangeEnvironment-! | '''Environment''' | ${port} |

!include <ChaskisTests.BuildingBlocks.StartServerAndChaskisWaitForConnection
*!
# Start Test

 * Ensure we if the version is not any different, we don't send a message.

| '''Wait For String''' | Bot\s+not\s+updated,\s+skipping\s+message | '''From Chaskis With Timeout''' |20000|

 * Ensure the file exists and still contains the correct version

| '''File''' | ${test_environment_path}/Plugins/NewVersionNotifier/.lastversion.txt | '''Contains''' | ${chaskis_version} |

!***> Teardown

!include <ChaskisTests.BuildingBlocks.StopServerAndChaskis
!include <ChaskisTests.BuildingBlocks.TeardownEnvironment
*!
